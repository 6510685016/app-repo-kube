spring:
  application:
    name: backend

server:
  port: 8765
  tomcat:
    threads:
      max: 400
    max-connections: 10000
    accept-count: 500

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true