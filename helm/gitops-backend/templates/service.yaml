apiVersion: v1
kind: Service
metadata:
  name: gitops-backend
spec:
  type: NodePort
  selector:
    app: gitops-backend
  ports:
    - name: http
      port: 8080          
      targetPort: 8080    
      nodePort: {{ .Values.service.nodePort }}  
