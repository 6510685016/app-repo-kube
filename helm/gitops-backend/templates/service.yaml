apiVersion: v1
kind: Service
metadata:
  name: gitops-backend
spec:
  type: ClusterIP
  selector:
    app: gitops-backend
  ports:
    - name: http
      port: 30000
      targetPort: 30000    
      nodePort: {{ .Values.service.nodePort }}  
